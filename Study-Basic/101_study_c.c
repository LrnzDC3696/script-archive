main() /*function main is always the first thing called in C*/
{ /*The things covers in curly brackets are the body of the function of in this case main*/
  printf("Hello you owo"); /*to print strings we use `"` instead of `'` which is for the ascii'*/
}